import { TalkDurationPipe } from './talk-duration.pipe';

describe('TalkDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new TalkDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
